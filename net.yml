---
- name: try ipaddr filter
  hosts: localhost
  gather_facts: false

  vars:
    value:
      - 192.24.2.1
      - host.fqdn
      - ::1
      - ''
      - 192.168.32.0/24
      - fe80::100/10
      - 42540766412265424405338506004571095040/64
      - true

  tasks:
    - name: use the ippadr filter
      ansible.builtin.debug:
        msg: "{{ value|ansible.utils.ipaddr('address') }}" 

...
